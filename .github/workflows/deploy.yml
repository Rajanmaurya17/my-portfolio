name: Deploy to GitHub Pages

on:
  push:
    branches the workflow file (`.github/workflows/deploy.yml`:
      - main

jobs:
  build:
    runs-on: ubuntu):

```yaml
name: Deploy to GitHub Pages

on:
  push-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 16

      - name: Install:
    branches:
      - main

jobs:
  build:
    runs dependencies
        run: npm install
        working-directory: ./ # Ensure this points to the correct-on: ubuntu-latest

    steps:
      - name: Checkout directory
